// // Define add function that takes two arguments and adds them up
// const add = (a, b) => a + b
// // Define subtract function that takes two arguments and subtracts them
// const subtract = (a,b) => a - b
// // Set up add as a default export

// // Set up subtract as a named export
// export { subtract, add as default }
// // Import both functions into indexx.js

// // Use both functions and print the results from each